/* ============================================================
   EDIT HERE to list real certifications. Intentionally empty —
   only add credentials you actually hold.
   Each entry once added: { name, issuer, year }
   ============================================================ */
window.STUDIO_CERTIFICATIONS = [];
